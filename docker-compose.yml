services:
  vpn:
    image: tobyxdd/hysteria
    container_name: hysteria_vpn
    restart: always
    volumes:
      - ./config.yaml:/etc/hysteria/config.yaml:ro
    command: ["client", "-c", "/etc/hysteria/config.yaml"]
    healthcheck:
      test: ["CMD-SHELL", "pgrep hysteria || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - autorpg_net

  postgres:
    image: postgres:16-alpine
    container_name: tg_autorpg_db
    restart: unless-stopped
    networks:
      - autorpg_net
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: autorpg
      POSTGRES_USER: autorpg
      POSTGRES_PASSWORD: "${DBPASS:-autorpg_pass}"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U autorpg"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  autorpg:
    build: .
    container_name: tg_autorpg
    restart: unless-stopped
    network_mode: "service:vpn"
    depends_on:
      vpn:
        condition: service_healthy
      postgres:
        condition: service_healthy
    mem_limit: 512m
    cpus: 0.5
    security_opt:
      - "no-new-privileges:true"

    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN:-placeholder}
      - ADMIN_IDS=${ADMIN_IDS:-}
      - HTTP_PROXY=http://127.0.0.1:8080
      - HTTPS_PROXY=http://127.0.0.1:8080
      - ALL_PROXY=socks5://127.0.0.1:1080
      - http_proxy=http://127.0.0.1:8080
      - https_proxy=http://127.0.0.1:8080
      - TELEGRAM_PROXY=${TELEGRAM_PROXY:-}
      - DBTYPE=postgresql+asyncpg
      - DBUSER=autorpg
      - "DBPASS=${DBPASS:-autorpg_pass}"  # ⚠️ Override via .env for production!
      - DBHOST=postgres
      - DBPORT=5432
      - DBNAME=autorpg
      - OFFLINE_TIMEOUT=${OFFLINE_TIMEOUT:-3600}
      - GAME_INTERVAL=${GAME_INTERVAL:-5}
      - USE_REDIS=${USE_REDIS:-false}
      - "REDIS_URL=${REDIS_URL:-redis://localhost:6379/0}"
      - HEALTH_PORT=${HEALTH_PORT:-8081}

    healthcheck:
      test: ["CMD", "python3", "/healthcheck.py"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  monitor:
    build: ./monitor
    container_name: tg_autorpg_monitor
    restart: unless-stopped
    networks:
      - autorpg_net
    ports:
      - "8082:8082"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      vpn:
        condition: service_healthy
    environment:
      - API_URL=http://vpn:8081
      - PORT=8082
      - COMPOSE_PROJECT_NAME=tg_autorpg
    security_opt:
      - "no-new-privileges:true"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    mem_limit: 256m
    cpus: 0.3

  vpn_monitor:
    build: ./vpn_monitor
    container_name: vpn_monitor
    restart: unless-stopped
    networks:
      - autorpg_net
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:rw
      - ./config.yaml:/etc/hysteria/config.yaml:rw
      - ./vpn_monitor/servers.json:/app/servers.json:ro
      - vpn_monitor_backups:/app/backups
    environment:
      - VPN_CONTAINER=hysteria_vpn
      - SOCKS5_PROXY=socks5://hysteria_vpn:1080
      - HTTP_PROXY=http://hysteria_vpn:8080
      - CHECK_INTERVAL=30
      - MAX_RESTART_ATTEMPTS=3
      - RESTART_COOLDOWN=120
      - SERVER_CHECK_TIMEOUT=3
      - VPN_CONFIG_PATH=/etc/hysteria/config.yaml
      - SERVERS_FILE=/app/servers.json
      - BACKUP_DIR=/app/backups
      - STARTUP_GRACE=20
      - CONSECUTIVE_FAILURES_REQUIRED=3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    mem_limit: 128m
    cpus: 0.2

networks:
  autorpg_net:
    driver: bridge

volumes:
  pgdata:
  vpn_monitor_backups:
